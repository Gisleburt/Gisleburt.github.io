name: Deploy to GitHub Pages

on:
  push:
    branches:
      - 2025

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "Dioxus Deploy"
        uses: DioxusLabs/deploy-action@0.1.2
        with:
          outDirectory: "target/dx/gisleburt-github-io/release/web/public"